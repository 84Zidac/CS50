#include <stdio.h>
//#include <cs50.h>

int main (void)
{
    char *s = "HI!";
    char *p = &s[1];
    printf("%p\n", p);
    printf("%p\n", s);






    // int n = 50;
    //int *p = &n;
    //printf("%p\n", p)
    // printf("%p\n", &n);// Prints memory location
    // printf("%i\n", *p);// Prints what is in that memory location
}